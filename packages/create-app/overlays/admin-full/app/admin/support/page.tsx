export default function SupportPage() {
  return (
    <>
      <h1 style={{ fontSize: "1.5rem", margin: "0 0 1rem" }}>Support</h1>
      <p style={{ color: "#6b7280", maxWidth: "60ch" }}>
        Ticket queue. Copied source — model it on whatever the client already
        uses rather than forcing them onto a shape the scaffold picked.
      </p>
    </>
  );
}
