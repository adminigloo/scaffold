{
  "name": "@adminigloo/email",
  "version": "0.1.1",
  "description": "Transactional email with a delivery log. With no API key a send is recorded as skipped instead of throwing, so every feature that sends mail still works before anyone has found a credential.",
  "license": "UNLICENSED",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adminigloo/scaffold.git",
    "directory": "packages/email"
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./schema": {
      "types": "./dist/schema.d.ts",
      "import": "./dist/schema.js",
      "require": "./dist/schema.cjs"
    }
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "public"
  },
  "dependencies": {
    "@adminigloo/db": "workspace:^",
    "@adminigloo/env": "workspace:^",
    "svix": "^1.99.1"
  },
  "peerDependencies": {
    "drizzle-orm": "^0.45.0",
    "zod": "^3.25.0 || ^4.0.0"
  },
  "devDependencies": {
    "drizzle-orm": "^0.45.2",
    "zod": "^4.5.1"
  },
  "scripts": {
    "build": "tsup src/index.ts src/schema.ts --format esm,cjs --dts --clean",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
